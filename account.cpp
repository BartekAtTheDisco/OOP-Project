#include <iostream>
#include "account.h"
#include <fstream>

using namespace std;


void Account::sign_in()
{/*
    string login, password;
    cout<<" login: ";
    cin>>login;
    cout<<" password: ";
    cin>>password;

    if((password==password_)&&(login==login_))
    {


    }

    fstream file;
    file.open("profile.txt",ios::is);
    getline(file, first);
    cout<<fisrt;
    file.close();*/
}


void Account::show_balance()
{

}
